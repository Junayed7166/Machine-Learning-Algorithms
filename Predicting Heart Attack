#Importing Necessary Libraries

import pandas as pd
from matplotlib import pyplot as plt
import numpy as np

#DATA READING AND UNDERSTANDING
from google.colab import files
uploaded = files.upload()

import io
data = pd.read_csv(io.BytesIO(uploaded['HeartAttackData.csv']))
data

x = data.iloc[:,0:13].values
y = data.iloc[:,13:14].values
x

# Import train_test_split function
from sklearn.model_selection import train_test_split

# Split dataset into training set and test set
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.10) # 90% training and 10% test
from sklearn.ensemble import RandomForestRegressor
regression = RandomForestRegressor(n_estimators = 100, random_state = 0)
regression.fit(x_train, y_train)

#from sklearn.metrics import r2_score
#r2_score(y_test, y_pred)
#Import scikit-learn metrics module for accuracy calculation
#Import Random Forest Model
from sklearn.ensemble import RandomForestClassifier

#Create a Gaussian Classifier
clf=RandomForestClassifier(n_estimators=100)

#Train the model using the training sets y_pred=clf.predict(X_test)
clf.fit(x_train,y_train)

y_pred=clf.predict(x_test)
from sklearn import metrics
# Model Accuracy, how often is the classifier correct?
print("This Model's Accuracy is:",metrics.accuracy_score(y_test, y_pred))

from sklearn.cluster import KMeans
from sklearn.preprocessing import LabelEncoder
from sklearn.preprocessing import MinMaxScaler
import seaborn as sns
import matplotlib.pyplot as plt
%matplotlib inline

g = sns.FacetGrid(data, col='Type')
g.map(plt.hist, 'Chest_Pain', bins=10)

data[['Sex','Chest_Pain','Type']].groupby(['Sex'], as_index=False).mean().sort_values(by='Type', ascending=False)
